<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- android:windowSplashScreenBackground is deliberately unset: the API 31+
         splash falls back to windowBackground while that is a single colour, so
         one attribute covers both the splash and the API 26-30 pre-window. If
         windowBackground ever becomes a drawable, the splash silently reverts to
         the system default and windowSplashScreenBackground has to be set. -->
    <style name="Theme.Okonomi" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/splash_background</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
